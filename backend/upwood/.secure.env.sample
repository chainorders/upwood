# Sensitive application secrets (copy to backend/upwood/.secure.env and fill values)
# These are used by backend-api service and must NOT be committed.

# Concordium wallet JSON strings
TREE_NFT_AGENT_WALLET_JSON_STR=
OFFCHAIN_REWARDS_AGENT_WALLET_JSON_STR=

# Optional Filebase/IPFS credentials (used if switching to Filebase)
FILEBASE_ACCESS_KEY_ID=
FILEBASE_SECRET_ACCESS_KEY=

