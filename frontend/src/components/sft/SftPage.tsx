import { Box, Typography } from "@mui/material";

export default function SftPage() {
	return (
		<Box
			sx={{
				display: "flex",
				flexDirection: "column",
				alignItems: "center",
				justifyContent: "center",
				height: "100%",
			}}
		>
			<Typography variant="h4">SFT Page</Typography>
		</Box>
	);
}
